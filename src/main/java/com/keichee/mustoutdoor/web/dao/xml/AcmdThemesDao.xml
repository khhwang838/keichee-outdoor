<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.keichee.mustoutdoor.web.dao.AcmdThemesDao">

    <insert id="insertTheme" >
		INSERT INTO MO_Themes (ACMD_THEME_ID, ACMD_THEME_NAME, ACMD_THEME_IMG_URL)
		VALUES (#{acmdThemeId}, #{acmdThemeName}, #{acmdThemeImgUrl})
	</insert>

    <update id="updateTheme" >
        UPDATE MO_Themes SET
		ACMD_THEME_NAME = #{acmdThemeName}
			<if test="themeImgUrl != null and themeImgUrl.length() > 0">
				, ACMD_THEME_IMG_URL = #{acmdThemeImgUrl}
			</if>
        WHERE ACMD_THEME_ID = #{acmdThemeId}
    </update>

    <delete id="deleteTheme">
		DELETE FROM MO_Themes WHERE ACMD_THEME_ID = #{acmdThemeId}
	</delete>

	<select id="selectAllThemes" resultType="com.keichee.mustoutdoor.web.domain.acmd.dto.AcmdThemesDto">
		SELECT
			ACMD_THEME_ID as acmdThemeId
			, ACMD_THEME_NAME as acmdThemeName
			, ACMD_THEME_IMG_URL as acmdThemeImgUrl
		FROM MO_Themes
	</select>
	
	<select id="selectThemesByAcmdUid" resultType="com.keichee.mustoutdoor.web.domain.acmd.dto.AcmdThemesDto">
		SELECT
			t.ACMD_THEME_ID as acmdThemeId
			, t.ACMD_THEME_NAME as acmdThemeName
			, t.ACMD_THEME_IMG_URL as acmdThemeImgUrl
		FROM MO_Themes t, MO_AcmdThemesRel r
		WHERE r.ACMD_UID = #{acmdUid} and t.ACMD_THEME_ID = r.ACMD_THEME_ID
	</select>
	
	<select id="selectThemesByUserId" resultType="com.keichee.mustoutdoor.web.domain.acmd.dto.AcmdThemesDto">
		SELECT
			t.ACMD_THEME_ID as acmdThemeId
			, t.ACMD_THEME_NAME as acmdThemeName
			, t.ACMD_THEME_IMG_URL as acmdThemeImgUrl
		FROM MO_Themes t, MO_AcmdThemesRel r
		WHERE r.USER_ID = #{userId} and t.ACMD_THEME_ID = r.ACMD_THEME_ID
	</select>
	
	<!-- 관계 테이블 관련 -->
    <insert id="insertAcmdThemesRel" >
		INSERT INTO MO_AcmdThemesRel (ACMD_UID, ACMD_THEME_ID, USER_ID)
		VALUES (#{acmdUid}, #{acmdThemeId}, #{userId})

	</insert>

    <insert id="insertAcmdThemesRels" >
		INSERT INTO MO_AcmdThemesRel(ACMD_UID, ACMD_THEME_ID, USER_ID)
		VALUES
		<foreach item="item" collection="list" open="" close="" separator=",">
		(#{item.acmdUid}, #{item.acmdThemeId}, #{item.userId})
		</foreach>
	</insert>

	<delete id="deleteAcmdThemesRelByAcmdUid">
		DELETE FROM MO_AcmdThemesRel
		WHERE ACMD_UID = #{acmdUid}
	</delete>

	<delete id="deleteAcmdThemesRelByUserId">
		DELETE FROM MO_AcmdThemesRel
		WHERE USER_ID = #{userId}
	</delete>
</mapper>